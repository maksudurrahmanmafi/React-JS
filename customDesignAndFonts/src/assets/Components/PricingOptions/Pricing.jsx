import { use } from "react";


const Pricing = ({pricingpromise}) => {
    const result = use(pricingpromise  )
    console.log(result);
    
    return (
        <div>
            
        </div>
    );
};

export default Pricing;